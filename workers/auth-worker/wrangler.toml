name = "alyx-tax-auth"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
ALLOWED_ORIGIN = "https://app.therapytaxapp.work"

[[kv_namespaces]]
binding = "TOKENS"
id = "11e7a0cd12ff476e975692b184740f58"

# Secrets are set via: npx wrangler secret put SECRET_NAME
# Required secrets:
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET

# Custom domain (auth.therapytaxapp.work) configured via Cloudflare dashboard after deploy
